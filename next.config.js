/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    PUBLIC_URL: "https://Alevtina98.github.io/animal-shelter-front",
    assetPrefix: './'
  }
}

module.exports = nextConfig

import React, { FC, memo } from "react";

const AppTab: FC = () => (
    <>
        <title> Любимчик </title>
        <link rel="icon" href="/img/logo0.png" />
    </>
);

export default memo(AppTab);
